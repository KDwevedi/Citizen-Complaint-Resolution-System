# Local-only artifacts — not for the public fork
.dev-logs/
.pull-logs/

# Generated by gen-compose.py / vite / npm
stack/docker-compose.local.yaml

# Local credentials override (config.env.example is the public version)

# Symlink-target node_modules / dist
